{% extends "index" %}

{% block content %}
    {% if entry.type == 'home' %}
        {% include '_pages/home.twig' %}
    {% else %}
        {% include '_includes/landing' %}
    {% endif %}
{% endblock %}